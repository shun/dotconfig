if &compatible
  set nocompatible " Be iMproved
endif

" load plugins
execute 'source' g:config_dir . '/rc.d/rc_dein.vim'

nnoremap <Space>b <Cmd>call ddu#start({'sources': [{'name': 'buffer', 'params': {}}]})<cr>

"function! Ddu_rg_find() abort
"  let word = input("search word: ")
"  call ddu#start({'sources': [{'name': 'rg', 'params': {'input': word}}]})
"endfunction

"command! DduRg call Ddu_rg_find()
nnoremap <Space>g <Cmd>DduRg<cr>
